{
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm -r --filter '!lib/replit-auth-web' run typecheck || echo 'skip typecheck errors'",
  "functions": {
    "api/index.ts": {
      "maxDuration": 10,
      "memory": 512
    }
  }
}
